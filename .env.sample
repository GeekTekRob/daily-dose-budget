# Server configuration
# Copy this file to `.env` and set real values for local development.

# The port and host for the Express API
PORT=4000
HOST=0.0.0.0

# SQLite database location (inside container or local path)
# For Docker Compose we mount ./data to /data, so default is /data/app.db
DB_PATH=./data/app.db

# JWT settings
JWT_SECRET=replace-with-a-strong-random-string
JWT_EXPIRES_IN=7d

# CORS origin (optional). Leave blank to allow all in dev or set explicitly.
# CORS_ORIGIN=http://localhost:5173

# Client configuration (Vite)
# If you serve the API on a different origin during dev, set this so the client hits it.
# In production the built client is served by the server and usually doesn't need this.
VITE_API_URL=http://localhost:4000
